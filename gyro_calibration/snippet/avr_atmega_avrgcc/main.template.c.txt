//include

#define GYRO_DOCALIBRATION 1 //enable or disable calibration
#define gyrocal_getrawdata(gxraw, gyraw, gzraw) gyroscope_getrawdata(gxraw, gyraw, gzraw); //set the function that return gyroscope raw values
#include "gyro_docalibration.h"

int main(void) {
    
	//init uart
	//init gyroscope
	
	#if GYRO_DOCALIBRATION  == 1
	gyrocal_docalibrationclient(uart_putc, uart_getc);
	#endif
	
	//your code
}
